{
  "name": "collaborative-doc-ai-copilot",
  "version": "0.1.0",
  "description": "Liveblocks-style collaborative document editor on Telnyx Edge Compute: a DocActor StatefulActor holds the document state, AgentSocketServer multiplayer syncs every participant over WebSocket, and the AI copilot proposes edits via the zero-credential Telnyx Inference binding.",
  "type": "module",
  "private": true,
  "scripts": {
    "local:dev": "vite-node scripts/local-dev.ts",
    "typecheck": "tsc --noEmit",
    "types": "telnyx-edge types",
    "ship": "telnyx-edge ship",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@telnyx/edge-runtime": "^0.9.2",
    "telnyx": "^7.13.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.18.0",
    "typescript": "^5.5.4",
    "vite-node": "^2.0.0",
    "vitest": "^2.0.0",
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
