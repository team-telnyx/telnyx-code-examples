# Edge secrets are managed with `telnyx-edge secrets add <name> <value>` per telnyx.toml.
# No local .env is required to run the example.
TELNYX_API_KEY=your_telnyx_api_key_here
AI_MODEL=meta-llama/Llama-3.3-70B-Instruct
