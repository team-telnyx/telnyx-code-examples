{
  "name": "patient-agent",
  "version": "1.0.0",
  "description": "A durable Telnyx Edge agent that acts as a living care coordinator for patients",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "dependencies": {
    "@telnyx/edge-sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "vitest": "^1.0.0",
    "wrangler": "^3.0.0"
  }
}
