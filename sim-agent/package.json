{
  "name": "sim-agent",
  "private": true,
  "version": "1.0.0",
  "description": "SIMAgent — The actor IS the SIM card. A durable Telnyx Edge Agent that tracks data usage, alerts on thresholds, and auto-provisions upgrades.",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "start": "npm run build && node scripts/start.mjs",
    "typecheck": "tsc --noEmit",
    "smoke": "node scripts/smoke.mjs"
  },
  "dependencies": {
    "@telnyx/edge-runtime": "*",
    "telnyx": "*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
