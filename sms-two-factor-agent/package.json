{
  "name": "sms-two-factor-agent",
  "version": "1.0.0",
  "description": "SMS Two-Factor Auth Agent built on Telnyx Edge — generate, send, verify, and expire codes via Agent SDK, KV, and the [telnyx] binding.",
  "main": "src/index.ts",
  "scripts": {
    "dev": "telnyx-edge dev",
    "deploy": "telnyx-edge ship",
    "types": "telnyx-edge types",
    "test": "node smoke_test.ts",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "telnyx",
    "sms",
    "2fa",
    "two-factor",
    "edge",
    "agent"
  ],
  "author": "Telnyx",
  "license": "MIT",
  "dependencies": {
    "@telnyx/edge-runtime": "^0.15.2"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0"
  }
}
