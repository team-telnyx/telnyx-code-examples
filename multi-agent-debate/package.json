{
  "name": "multi-agent-debate",
  "version": "1.0.0",
  "description": "Two AI agents debate a topic with live WebSocket streaming and audience voting",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "dependencies": {
    "@telnyx/edge-sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^0.34.0",
    "wrangler": "^3.0.0"
  }
}
