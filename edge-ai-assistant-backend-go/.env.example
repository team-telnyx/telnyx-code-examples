# Telnyx API key — required for fetching the org public key
TELNYX_API_KEY=your_telnyx_api_key_here

# Public key for webhook signature verification (fetched via API, stored as Edge secret)
# telnyx-edge secrets add TELNYX_PUBLIC_KEY "<base64-key>"
TELNYX_PUBLIC_KEY=your_telnyx_public_key_here
