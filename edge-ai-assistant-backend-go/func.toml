[edge_compute]
# func_id and func_name are generated by `telnyx-edge new-func` at scaffold time.
# Uncomment and replace with your own values after scaffolding.
# func_id = "your-func-id-here"
# func_name = "your-func-name-here"

# For environment variables and secrets:
# Use telnyx-edge secrets add <name> <value>
# Secrets are injected as environment variables into all your functions
